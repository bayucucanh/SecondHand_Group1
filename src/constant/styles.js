import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  card: {
    borderRadius: 8,
    padding: 12,
    backgroundColor: 'white',
    elevation: 4,
    marginBottom: 10,
  },
});
